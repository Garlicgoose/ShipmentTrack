# Development Requirements

The following requirements are mandatory and must be followed strictly:

1. Every change must have its own Git commit. Do not leave completed changes uncommitted.
2. Every change must add or update appropriate tests.
3. Before committing, run all relevant tests and validation checks and ensure they pass.
